<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>{{ title || 'SyncMA' }}</title>

    {{-- Slot untuk meta tag tambahan --}}
    @if ($slots.meta)
      {{{ await $slots.meta() }}}
    @endif

    <!-- ✅ Tailwind Play CDN (support full utilities) -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- ✅ Konfigurasi warna opsional -->
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              cyan: tailwind.colors.cyan,
              gray: tailwind.colors.gray,
            }
          }
        }
      }
    </script>
  </head>
  <body class="bg-white text-gray-800">
    {{{ await $slots.main() }}}
  </body>
</html>
